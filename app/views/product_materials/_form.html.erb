<%= form_for(@product_material) do |f| %>
  <% if @product_material.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_material.errors.count, "error") %> prohibited this product_material from being saved:</h2>

      <ul>
      <% @product_material.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :units %><br>
    <%= f.number_field :units %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
